spring.application.name=statistic-service

spring.config.import=config-server:http://localhost:8888
# read config from server localhost 8888
spring.cloud.config.fail-fast=true
# the server will  not run if if cant search the config-server